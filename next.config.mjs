/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/curators-coffee",
  images: { unoptimized: true },
};

export default nextConfig;
